<?xml version="1.0" encoding="utf-8"?>
<!-- File: app/src/main/res/layout/fragment_home.xml
     Fungsi: layout beranda (Home) menampilkan toolbar, RecyclerView (produk), SwipeRefresh, dan FAB -->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/white"
            app:title="UniTrade"
            app:titleTextColor="@color/black"
            app:menu="@menu/home_top_menu"
            android:elevation="4dp"
            android:contentDescription="Toolbar UniTrade" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Note: removed layout_marginTop to avoid large gap; CoordinatorLayout will place content under AppBar -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvProducts"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="8dp"
            android:paddingBottom="80dp"
            android:scrollbars="vertical"
            tools:listitem="@layout/item_product_card" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Floating Action Button (ditinggikan sedikit agar tidak tertutup BottomNav) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddProduct"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/ic_live_help"
        android:contentDescription="FAQ Chatbot"
        app:tint="@android:color/white"
        app:backgroundTint="@color/btnOrange"
        app:layout_anchor="@id/swipeRefresh"
        app:layout_anchorGravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="48dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
