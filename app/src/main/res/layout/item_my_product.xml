<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_my_product.xml -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    android:layout_margin="8dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageView
            android:id="@+id/imgProductMy"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/product_image" />

        <TextView
            android:id="@+id/tvTitleMy"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/imgProductMy"
            android:layout_marginStart="12dp"
            android:text="Judul Produk"
            android:maxLines="2"
            android:ellipsize="end"
            android:textAppearance="?attr/textAppearanceBodyMedium" />

        <TextView
            android:id="@+id/tvPriceMy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/imgProductMy"
            android:layout_below="@id/tvTitleMy"
            android:layout_marginStart="12dp"
            android:layout_marginTop="6dp"
            android:text="Rp 0"
            android:textStyle="bold"
            android:textAppearance="?attr/textAppearanceBodySmall" />

        <TextView
            android:id="@+id/tvConditionMy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/tvPriceMy"
            android:layout_alignBaseline="@id/tvPriceMy"
            android:layout_marginStart="8dp"
            android:text="Like New"
            android:textAppearance="?attr/textAppearanceLabelSmall" />

        <!-- Status badge (Tersedia/Terjual) -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chipStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/imgProductMy"
            android:layout_below="@id/tvPriceMy"
            android:layout_marginStart="12dp"
            android:layout_marginTop="8dp"
            android:text="Tersedia"
            android:textSize="11sp"
            app:chipBackgroundColor="@color/green_500"
            android:textColor="@android:color/white" />

        <!-- action buttons on the right -->
        <LinearLayout
            android:id="@+id/actionButtons"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true">

            <ImageButton
                android:id="@+id/btnMarkSold"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_check_circle"
                android:contentDescription="Mark as sold"
                android:tint="@color/green_500" />

            <ImageButton
                android:id="@+id/btnEditProduct"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_edit"
                android:contentDescription="@string/edit"
                android:tint="?attr/colorOnSurface" />

            <ImageButton
                android:id="@+id/btnDeleteProduct"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_delete"
                android:contentDescription="@string/delete"
                android:tint="?attr/colorError" />
        </LinearLayout>

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>
